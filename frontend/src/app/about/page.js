import React from 'react';

const page = () => {
    throw new Error("this an error ");
    return (
        <div>
            about page content
            
        </div>
    );
};

export default page;